import { ProjectSubTaskFilterPipe } from './project-sub-task-filter.pipe';

describe('ProjectSubTaskFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectSubTaskFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
